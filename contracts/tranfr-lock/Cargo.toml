[package]
name = "tranfr-lock"
version = "0.1.0"
edition = "2024"

[dependencies]
ckb-std = "1.1"
since-cmp = { path = "../../reference/since-cmp-rs" }

[features]
library = []
native-simulator = ["library", "ckb-std/native-simulator"]
